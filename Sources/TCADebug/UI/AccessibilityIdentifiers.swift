/// Stable accessibility identifiers for the package's UI, exposed so UI tests
/// (page objects) can target the floating button and console reliably.
public enum TCADebugAccessibility {
  public static let floatingButton = "tca-debug.floating-button"
  public static let console = "tca-debug.console"
  public static let closeButton = "tca-debug.close-button"
}
